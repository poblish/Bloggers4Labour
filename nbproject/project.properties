application.args=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=dist/Bloggers4Labour.jar
dist.javadoc.dir=${dist.dir}/javadoc
jar.compress=true
javac.classpath=\
    ${libs.servlet23.classpath}:\
    ${libs.JDOM.classpath}:\
    ${libs.Informa0.6.5.classpath}:\
    ${libs.Log4J.classpath}:\
    ${libs.Hiatus_Libraries.classpath}:\
    ${libs.JavaMail.classpath}:\
    ${libs.XML.classpath}:\
    ${libs.Lucene.classpath}:\
    ${libs.MySQL.classpath}:\
    ${libs.HTMLParser.classpath}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:unchecked
javac.deprecation=true
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.Hiatus_Libraries.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.Hiatus_Libraries.classpath=../../Biorhythms X folder/Web Application/web-app/WEB-INF/lib/bio_cpa.jar:../../Biorhythms X folder/Web Application/web-app/WEB-INF/lib/b4l_webapp.jar:../International/icu4j_3_2.jar
# Property libs.HTMLParser.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.HTMLParser.classpath=../htmlparser1_6/lib/htmlparser.jar
# Property libs.Informa0.6.5.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.Informa0.6.5.classpath=../../RSS/Informa/informa-0.6.5/build/lib/informa.jar
# Property libs.Informa0.6.5_Runtime.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.Informa0.6.5_Runtime.classpath=../../RSS/Informa/informa-0.6.5/lib/commons-beanutils.jar:../../RSS/Informa/informa-0.6.5/lib/commons-collections.jar:../../RSS/Informa/informa-0.6.5/lib/commons-lang.jar:../../RSS/Informa/informa-0.6.5/lib/commons-logging.jar:../../RSS/Informa/informa-0.6.5/lib/dom4j.jar:../../RSS/Informa/informa-0.6.1/lib/jdbc2_0-stdext.jar:../../RSS/Informa/informa-0.6.5/lib/jdom.jar:../../RSS/Informa/informa-0.6.5/lib/xdoclet.jar
# Property libs.JDOM.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JDOM.classpath=../../RSS/Informa/informa-0.6.5/lib/jdom.jar
main.class=org.bloggers4labour.Launcher
platform.active=Java_HotSpot_TM__Client_VM_1.5.0_06-56
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.Hiatus_Runtime.classpath}:\
    ${libs.Informa0.6.5_Runtime.classpath}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-Xmx100m -Dcom.hiatus.site=bloggers4labour -Dcom.sun.management.jmxremote
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=src
test.src.dir=test
